// Generated by tool.

import 'package:music_notation/src/smufl/smufl_glyph.dart';

{% for className in classes %}
part './classes/{{ className|toUnderscoreCase }}.dart';
{% endfor %}

/// Group of glyph classes.
sealed class GlyphClasses {
  {% for className in classes %}
  static const {{ className|normalizeClassName }} = {{ className|normalizeClassName|capitalizeFirst }};
  {% endfor %}
}

/// Grouped glyphs together, so that software developers can handle similar
/// glyphs (e.g. noteheads, clefs, flags, etc.) in a similar fashion.
sealed class GlyphClass {
  List<SmuflGlyph> get values;
}

