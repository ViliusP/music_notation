// Generated by tool.

part of "../glyph_class.dart";

class {{ name|capitalizeFirst}} extends GlyphClass {
  @override
  List<SmuflGlyph> get values => [
    {% for glyph in glyphs %}
    {% if glyph[0] in '0123456789' %}
      {% set enumName = "g" + glyph %}
    {% else %}
      {% set enumName = glyph %}
    {% endif %}
    SmuflGlyph.{{ enumName }},
    {% endfor %}
  ];

  {% for glyph in glyphs %}
  {% if glyph[0] in '0123456789' %}
    {% set enumName = "g" + glyph %}
  {% else %}
    {% set enumName = glyph %}
  {% endif %}
  static const {{ enumName }} = SmuflGlyph.{{ enumName }};

  {% endfor %}
}

